apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backend-ab.fullname" . }}
  labels:
    {{- include "backend-ab.labels" . | nindent 4 }}
data:
  # nginx:alpine runs envsubst on /etc/nginx/templates/*.template at startup,
  # rendering to /etc/nginx/conf.d/. $APP_NAME / $APP_VERSION come from the
  # pod's env, so the version is frozen at pod creation and does NOT change
  # when this ConfigMap is edited in place.
  default.conf.template: |
    server {
      listen {{ .Values.service.port }};
      server_name _;

      location = /api/ {
        default_type application/json;
        return 200 '{"app":"${APP_NAME}","version":"${APP_VERSION}"}';
      }

      location = /healthz/ {
        default_type text/plain;
        return 200 'ok';
      }
    }
